import SwiftUI

struct ThreadsView: View {
    var body: some View {
        Text("Threads Feed Placeholder")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.purple.opacity(0.3))
    }
}

#Preview {
    ThreadsView()
}

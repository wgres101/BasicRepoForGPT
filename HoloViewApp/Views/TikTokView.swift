import SwiftUI

struct TikTokView: View {
    var body: some View {
        Text("TikTok Feed Placeholder")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.red.opacity(0.3))
    }
}

#Preview {
    TikTokView()
}

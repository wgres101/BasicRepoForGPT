import SwiftUI
import CoreLocation

struct FindMeView: View {
    var body: some View {
        Text("FindMe Placeholder: show friends on a map")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color.orange.opacity(0.3))
    }
}

#Preview {
    FindMeView()
}
